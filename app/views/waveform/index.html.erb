<div data-controller="waveform">
  <h1 class="waveform-h1">Javascript Waveform viewer</h1>
  <p class="waveform-p">
    This is a waveform viewer I made while researching audio compression
    algorithms for the
    <a
      href="https://content.neuralink.com/compression-challenge/README.html"
      target="_blank"
      class="waveform-link-2"
      rel="noopener"
    >NeuraLink compression challenge.</a>
  </p>
  <div class="wf-controls" id="controls">
    <label for="fileInput">Upload .wav File:</label>
    <input type="file" id="fileInput" accept=".wav" autocomplete="off">
    <!-- Add this element to display the filename -->
    <div
      id="fileNameDisplay"
      style="display: none; margin-top: 10px; font-weight: bold;"
    ></div>

    <label for="startTime">Start Time (s):</label>
    <input
      type="number"
      id="startTime"
      value="0"
      step="0.01"
      min="0"
      autocomplete="off"
    >
    <label for="endTime">End Time (s):</label>
    <input
      type="number"
      id="endTime"
      value="1"
      step="0.01"
      min="0"
      autocomplete="off"
    >
    <button id="drawButton">Draw Waveform Section</button>
  </div>
  <canvas id="canvas" class="wf-canvas"></canvas>
</div>
