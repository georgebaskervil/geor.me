# async: in-process (default for single Iodine worker). For multiple workers, switch
# production to the redis adapter, add the redis gem, and set REDIS_URL.
development:
  adapter: async

test:
  adapter: test

production:
  adapter: async
